Отлично! Вот МАКСИМАЛЬНО КОРОТКАЯ ИНСТРУКЦИЯ по запуску:

🚀 ЗАПУСК ЗА 5 МИНУТ
1. Открыть терминал (PowerShell или CMD)
2. Клонировать и зайти в папку
   bash
   git clone https://github.com/gareq1211/ITQ-document-flow.git
   cd ITQ-document-flow
3. Запустить базу данных
   bash
   docker-compose up -d
   Подождать 10 секунд

4. Запустить сервис
   bash
   cd service
   gradle bootRun
   Подождать 20 секунд

5. Открыть браузер и проверить
   А) Проверка что сервис жив:

http://localhost:8080/actuator/health
→ Должен увидеть: {"status":"UP"}

Б) Создать первый документ:

bash
curl -X POST http://localhost:8080/api/documents -H "Content-Type: application/json" -d "{\"author\":\"Тест\",\"title\":\"Док\"}"
→ Вернёт JSON с id документа (например {"id":1,...})

В) Посмотреть документ:

http://localhost:8080/api/documents/1
→ Увидишь документ с историей

Г) Swagger UI (удобный интерфейс):

http://localhost:8080/swagger-ui.html
→ Там можно тыкать кнопки и тестировать API

Д) pgAdmin (база данных):

http://localhost:5050

Логин: admin@admin.com

Пароль: admin

Подключиться к серверу:

Host: postgres

Порт: 5432

База: document_flow

Пользователь: postgres

Пароль: postgres

✅ Что ты увидишь:
В терминале - логи запуска, потом "Started DocumentFlowApplication"

В браузере - JSON ответы от API

В Swagger - красивые кнопки для всех запросов

В pgAdmin - таблицы: document, document_history, approval_registry